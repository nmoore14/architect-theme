{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Architect Theme",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/test/TypeScript.ts"
      ]
    }
  ]
}
